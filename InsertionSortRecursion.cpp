#include<iostream>
using namespace std;
void sortArray(int *arr , int size)
{
     if(size==0 || size==1)
    {
        return ;
    }
    for(int i=1;i<size;i++)
    {
        
    }
    sortArray(arr,size-1);
}